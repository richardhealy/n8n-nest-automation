{
  "name": "n8n-project",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "biome format --write .",
    "lint": "biome lint --apply .",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "typescript": "^5.0.0"
  }
}
